{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalDependencies": ["pnpm-workspace.yaml", "packages/config/**"],
  "tasks": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**", "out/**"],
      // Inlined into the client bundle at build time, so it belongs in the hash:
      // a different endpoint is a different build.
      "env": ["NEXT_PUBLIC_VITALS_ENDPOINT"]
    },
    "typecheck": {},
    "test": {},
    "lighthouse": {
      "dependsOn": ["build"],
      // Starts a server and uploads a report; there is nothing here to replay.
      "cache": false,
      "passThroughEnv": ["LHCI_*", "CHROME_PATH", "CI"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "fetch:news": {
      "cache": false
    }
  }
}
