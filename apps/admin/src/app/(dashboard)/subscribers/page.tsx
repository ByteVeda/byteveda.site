import type { Metadata } from "next";

export { SubscribersPage as default } from "@/features/subscribers";

export const metadata: Metadata = { title: "Subscribers" };
export const dynamic = "force-dynamic";
