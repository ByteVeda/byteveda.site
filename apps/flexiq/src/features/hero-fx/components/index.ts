/**
 * The hero field is a visual effect and nothing else — no server API, so the
 * components barrel is the feature's only door. The tier detector and the
 * scatter hash it picks between stay private to the fields that draw them.
 */

export { HeroField } from "./hero-field";
