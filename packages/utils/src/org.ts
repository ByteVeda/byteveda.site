/**
 * Canonical ByteVeda identifiers. Every app derives its own `site` config from
 * these, so a domain or org rename happens in exactly one place.
 */
export const ORG = "ByteVeda";

export const GITHUB_URL = `https://github.com/${ORG}`;

export const MAIN_DOMAIN = "byteveda.org";
export const MAIN_URL = `https://${MAIN_DOMAIN}`;

export const DOCS_DOMAIN = "docs.byteveda.org";
export const DOCS_URL = `https://${DOCS_DOMAIN}`;

export const CONTACT_EMAIL = `hello@${MAIN_DOMAIN}`;
